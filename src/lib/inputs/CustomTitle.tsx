import styled from 'styled-components';

const CustomTitle = styled.h1`
  margin-right: auto;
  text-align: left;
  font-size: 30px;
  color: black;
  font-weight: 600;
`;

export default CustomTitle;
